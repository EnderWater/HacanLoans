# To transpile the app
Run: npm run build

# To generate styles
To generate new tailwind styles, run: npm run build-styles

# To add custom styles
Write all of your custom styles in "input.css". Running the above command to generate the styles will take those custom styles and place them into styles.css.